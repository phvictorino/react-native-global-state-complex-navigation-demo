import { StyleSheet } from "react-native";

export const userRegistrationStyles = StyleSheet.create({
  container: {
    paddingTop: 16,
    flex: 1,
    gap: 16,
    alignItems: "stretch",
    paddingHorizontal: 16,
  },
});
